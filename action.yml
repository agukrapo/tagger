name: 'Tagger'
description: 'auto pushes a git tag according to its commits messages'
inputs:
  token:
    description: 'Repository github token'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    GITHUB_TOKEN: ${{ inputs.token }}
